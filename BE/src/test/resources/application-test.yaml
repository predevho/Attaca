spring:
  datasource:
    url: jdbc:h2:mem:attaca-test;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop

storage:
  local:
    root-dir: build/test-uploads
