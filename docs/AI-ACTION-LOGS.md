# AI-ACTION-LOGS

최근 작업 로그. 최대 100개 유지 (오래된 항목부터 제거).

---

* 2026-07-05 — 프로젝트 초기 설계 브레인스토밍 진행. 서비스 정의(음악인 커뮤니티 SNS), 도메인 6개, 기술 스택 확정.
* 2026-07-05 — `docs/` 문서 체계 생성: ARCHITECTURE(CONSTITUTION/STATUTE), DOMAIN-COMMON(CONSTITUTION/STATUTE), DOMAIN-MEMBER(CONSTITUTION/STATUTE), TODO-*, CONTEXT, AI 기록 문서.
* 2026-07-05 — 플러그인 설치 검증(context7/jdtls-lsp/typescript-lsp/claude-md-management 적용 확인, supabase는 미인증). `BE/build.gradle.kts` Spring Boot 4.1.0→3.4.5 재조정 + 스타터명 수정, Gradle 래퍼 9.5.1→8.11.1, 테스트용 H2 추가. `./gradlew clean build` 통과.
* 2026-07-05 — BE 전역 기반 구성 TDD 완료: `ApiResponse`, `BaseEntity`(+JpaAuditingConfig), `ErrorCode`/`BusinessException`/`GlobalExceptionHandler`. 테스트 4종 + 전체 빌드 통과. VSCode cSpell 한글 오탐 → `.vscode/settings.json`에 Hangul ignore 규칙 추가.
* 2026-07-05 — 기존 전역 코드 상세 리뷰(기능/근거/대안) 후 두 가지 반영: (1) `ErrorCode`에 `resultCode`(int, HTTP 기반 40001/40501/50001) 추가하고 `ApiResponse.ErrorBody`를 `(resultCode, code, message)`로 확장(Swagger/Postman 문서화 대비, 응답 본문 노출). (2) 손으로 쓴 필드 getter를 Lombok `@Getter`로 전환(ApiResponse/BaseEntity/BusinessException/ErrorCode). TDD(RED: `resultCode()` 심볼 없음 → GREEN: `clean build` 통과). `ErrorCode` 인터페이스화는 개발 진행하며 판단하기로 보류. (커밋 `d459be0`)
* 2026-07-05 — `ErrorCode` 수동 생성자를 Lombok `@RequiredArgsConstructor`로 대체(수동 생성자와 중복 정의 충돌 확인 후 제거). 동작/응답 포맷 변경 없음, `clean build` 통과. (커밋 `4a03d3a`)
* 2026-07-05 — 설계 유지 결정 논의: (1) `JpaAuditingConfig` 분리 구조 유지 — `@DataJpaTest` 슬라이스 테스트에서 `@Import`로 감사 설정을 재사용하기 위함(진입점 부착은 슬라이스에서 감사 비활성 문제). (2) `ApiResponse` 현재 설계 유지 — 타 프로젝트의 평면 record(`resultCode` 문자열 `"200-1"` 방식)와 비교했으나, 도메인 다수·에러 중앙관리(`ErrorCode` enum) 이점으로 현행(success 불리언 + data/error 분리) 유지.
* 2026-07-05 — TIL 문서 체계 시작: `docs/TIL/` 신설, `docs/TIL/2026-07-05-generics.md`(Java 제네릭 상세) 작성. 학습 기록용이며 코드/설계에 영향 없음.
* 2026-07-05 — 노션 프로젝트 허브 구성(`AIBE6 기록` 워크스페이스): "Attaca 프로젝트" 페이지 + TODO/TIL/스케줄 DB(보드·캘린더 뷰) + 프로젝트 문서 4종. `CLAUDE.md`에 노션 동기화 규칙 추가.
* 2026-07-05 — `resultCode` 포맷 변경: `int 40001` → `String "400-01"`(HTTP상태-일련번호, 가독성). `ErrorCode`/`ApiResponse.ErrorBody`/테스트/문서 반영, TDD(RED→GREEN) 후 `clean build` 통과.
* 2026-07-11 — BE 보안 기반(Security+JWT) 골격 구현 완료(계획 Task1~5, TDD). `Role`/인증 ErrorCode 7종, `JwtProvider`/`JwtProperties`(jjwt 0.12.6), `JwtAuthenticationFilter`, `SecurityConfig`(STATELESS)+핸들러 2종, `AuthController(/api/auth/reissue)`. access+refresh 무상태(refresh에도 role claim). 테스트 5종 + `clean build` 통과. 커밋 5개(`5a76833`~`6da73cf`).
* 2026-07-12 — 날짜 오기 정정. 보안 골격 기록을 실제 커밋일 `07-11`로 수정(TODO-DONE, ACTION-LOGS). TIL은 작성일 기준 `07-12`로 통일(파일명 `2026-07-07-...`→`2026-07-12-...`, 본문 작성일, 노션 TIL `날짜` 속성). git 이력·실제 커밋일과 문서 날짜 불일치 해소.
* 2026-07-12 — MEMBER 자체 회원가입/로그인 TDD 구현. `domain.member` 계층(`Member`/`MemberRepository`/`MemberService`/`MemberAuthController`) + DTO 4종. `POST /api/auth/signup`(중복 email/nickname 검증, BCrypt 해시)·`POST /api/auth/login`(access+refresh 발급). 전역 `ErrorCode`에 401-07/409-01/409-02 추가. `SecurityConfigTest`가 `@WebMvcTest`로 신규 컨트롤러를 끌어와 컨텍스트 로딩 실패 → 스캔 범위를 TestController로 한정해 회귀 수정. 전체 `clean build` 통과(43개 테스트). 소셜 로그인·프로필은 범위 밖(BACKLOG).
* 2026-07-13 — MEMBER 카카오 소셜 로그인 + 자체 로그인 loginId 전환(브레인스토밍→스펙→계획→subagent-driven 실행, 브랜치 `feature/member-oauth2-social-login`). 식별자 역할 분리(loginId/email/nickname/id), 자체 auth를 email→loginId 개정. 카카오 `POST /api/auth/oauth/kakao`(프론트 인가코드→백엔드 교환, `OAuthClient`/`KakaoOAuthClient`, `SocialAccount`). 검증 이메일만 자동연결·미검증 거절(탈취 방지, 회귀 테스트 포함), 신규 자동가입·nickname 유니크 생성. 에러코드 409-03/401-08/502-01 추가, `LOGIN_FAILED` 문구 아이디 기준 정정. 6개 태스크 TDD+태스크별 리뷰(리뷰 지적 2건: 탈취 회귀 테스트·userinfo null 가드 수정 반영), 전체 `clean build` 통과. 카카오 실HTTP는 env 키 수동 검증(자동 테스트는 매핑·로직만).
* 2026-07-14 — 파일 저장 기반(FileStorage/FileService/FileMetadata) TDD 구현(브랜치 `feature/be-file-storage`). `global.storage`: `FileStorage`(인터페이스)/`LocalFileStorage`(기본)/`S3FileStorage`(AWS SDK v2)/`StorageProperties`, `FileService.upload(file, directory, uploaderId)`가 key 생성+저장+메타데이터 영속화를 트랜잭션으로 조합. 에러코드 4종 추가: `INVALID_FILE`(400-02)/`FILE_NOT_FOUND`(404-01)/`FILE_UPLOAD_FAILED`(500-02)/`RESOURCE_NOT_FOUND`(404-02, 일반). `/files/**` permit(SecurityConfig). 리뷰 중 버그 2건 발견·수정: (1) 앱 전역 500→404 라우팅 버그 — `NoResourceFoundException`이 catch-all에 걸려 500으로 응답되던 것을 `RESOURCE_NOT_FOUND` 전용 핸들러로 404 정정, (2) 확장자 파서가 dotfile(점이 index 0)에서 원본 파일명 전체를 key로 흘려보내던 취약점 수정. 메타데이터 구조 문서 충돌을 공용 `FileMetadata` 테이블로 해소(`DOMAIN-COMMON-STATUTE §7`). 전체 `clean build` 통과. 실제 S3 연동은 자격증명 발급 후 수동 검증 예정.
* 2026-07-15 — 전체 문서 점검·첨삭: MEMBER-STATUTE §3 도입부의 낡은 email 로그인 서술 정정(loginId 기준), MemberProfile 초안의 `profileImageUrl` 필드 제거(COMMON §7 "URL 미저장" 규칙과 충돌), COMMON-STATUTE §8 위치 정리(§2와 §3 사이→끝). 일정 반영: 런타임 DB 미구성으로 `bootRun` 불가 사실을 CONTEXT에 명시하고 READY에 추가(FE 연동 선행 조건), MEMBER 프로필/이미지 BACKLOG→READY(FileStorage 완료), CORS/Swagger/refresh 로테이션을 BACKLOG 신설.
* 2026-07-15 — 런타임 DB(MySQL docker-compose) + MEMBER 프로필/이미지 TDD 구현(브랜치 feature/be-runtime-db-member-profile). 테스트 H2 프로파일 분리, Bean Validation 도입, 클라이언트 실수 3종 400 매핑(기존 500 결함 수정), MemberProfile+Instrument(21종)+API 4종. 전체 clean build 통과.
* 2026-07-15 — FE Next.js 초기화 + 인증 플로우(BFF) TDD 구현(브랜치 feature/fe-init-auth-bff). Next16/React19/TS/Tailwind/Vitest. BFF 3계층으로 토큰을 UI에서 격리(httpOnly 쿠키), 회원가입/로그인/대시보드 + 미들웨어 보호 + reissue 1회 재시도. 통신은 네이티브 fetch, CORS는 BFF라 미추가. build+vitest 통과.
* 2026-07-15 — FE 인증 플로우 수동 E2E 검증 완료(MySQL+BE bootRun+FE dev 실기동, 브라우저). 가입→/login 리다이렉트, 로그인→/dashboard "환영합니다"(인증 `/api/bff/me` 성공), **토큰이 JS에 미노출(document.cookie·localStorage 비어 있음 — httpOnly 확인)**, 로그아웃→쿠키삭제→/login, 로그아웃 후 /dashboard 접근→미들웨어가 /login 리다이렉트. 6단계 모두 정상. 발견: 사용자 환경변수 `DB_PASSWORD=1234`가 docker-compose 기본값을 덮어써 bootRun이 MySQL 접속 거부됨 → `--args=--spring.datasource.password=attaca-local`로 override해 해결(시스템 env 미변경). 이전 `Access denied`(2026-07-15 DB 태스크)의 진짜 원인도 이것으로 확인.
* 2026-07-15 — VERIFIED-PERFORMER 도메인 문서화(브레인스토밍→설계 승인→CONSTITUTION/STATUTE 작성). 인증 상태를 Member boolean이 아닌 별도 엔티티 `VerificationApplication`으로 소유, 상태 4종(PENDING/APPROVED/REJECTED/REVOKED), 재신청=새 레코드(이력 보존), 활성 신청 유일성(PENDING/APPROVED 시 신규 거절), 어드민 직접지정. 심사는 `/api/admin/**`(ROLE_ADMIN). 뱃지는 MEMBER `ProfileResponse.verified`로 파생(`isVerified` 서비스 협력). 에러코드 409-04~06/404-04(구현 시 추가). 공개목록/첨부파일/이력테이블 범위 밖. 코드 구현은 다음 작업, 문서만 완료.
* 2026-07-15 — FE 카카오 소셜 로그인 연동 TDD 구현(브랜치 feature/fe-kakao-oauth). 서버 start/callback BFF 라우트로 완전 서버측 처리(콜백은 라우트 핸들러), CSRF state 서버 생성·httpOnly 쿠키·단일사용 대조. `/login` 카카오 버튼+에러 안내(서버 페이지+클라이언트 폼 분리). `oauthState`/`kakao` 헬퍼. Vitest 목 테스트 통과, 실제 카카오 왕복은 앱 키 미확보로 배선만 로컬 검증(BE 카카오와 동일 전략).
